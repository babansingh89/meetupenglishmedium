
@{
    ViewBag.Title = "Shift";
    Layout = "~/Views/Shared/_SuperAdminLayout.cshtml";
}


<style>
    .wrapper { padding: 0 !important; margin-top: 64px !important; }
    #div_Scroll { margin: 0 !important; }
    #div_Scroll .col-lg-12 { padding: 0 !important; }

    .card-modern {
        background: #fff;
        border-radius: 0;
        box-shadow: none;
        border: none;
        overflow: hidden;
    }
    .card-header-modern {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        border-bottom: 2px solid #0089b3;
        padding: 10px 16px;
        display: flex;
        align-items: center;
        gap: 12px;
    }
    .card-header-modern .header-icon {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 36px;
        height: 36px;
        background: linear-gradient(135deg, #0089b3, #006d8f);
        border-radius: 8px;
        color: #fff;
        font-size: 18px;
        flex-shrink: 0;
    }
    .card-header-modern .header-title {
        font-size: 18px;
        font-weight: 700;
        color: #0f172a;
        letter-spacing: -.2px;
    }
    .card-header-modern .btn-register {
        margin-left: auto;
        background: linear-gradient(135deg, #0089b3, #006d8f);
        border: none;
        border-radius: 6px;
        color: #fff;
        font-size: 13px;
        font-weight: 600;
        padding: 8px 18px;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        transition: all .2s;
        box-shadow: 0 2px 8px rgba(0,137,179,.2);
    }
    .card-header-modern .btn-register:hover {
        background: linear-gradient(135deg, #006d8f, #004d66);
        color: #fff;
        box-shadow: 0 4px 14px rgba(0,137,179,.3);
        -webkit-transform: translateY(-1px);
        transform: translateY(-1px);
    }
    .card-body-modern {
        padding: 16px 20px 24px;
        font-family: 'Roboto', sans-serif;
    }

    #tblclass {
        width: 100% !important;
        margin: 0;
        border-collapse: collapse;
        border: 1px solid #e2e8f0;
    }
    #tblclass thead th {
        background: linear-gradient(135deg, #0089b3, #006d8f);
        color: #fff;
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: .6px;
        padding: 12px 18px !important;
        border: none !important;
        border-right: 1px solid rgba(255,255,255,.08) !important;
        vertical-align: middle !important;
        white-space: nowrap;
    }
    #tblclass thead th:last-child { border-right: none !important; }
    #tblclass tbody td {
        padding: 10px 18px !important;
        font-size: 13px;
        color: #1e293b;
        border-bottom: 1px solid #f1f5f9 !important;
        border-right: none !important;
        vertical-align: middle !important;
        white-space: nowrap;
    }
    #tblclass tbody tr:nth-child(even) { background: #fafcfe; }
    #tblclass tbody tr:hover { background: #eef6ff; }
    #tblclass i.fa {
        color: #64748b;
        margin-right: 10px;
        transition: color .2s;
        cursor: pointer;
    }
    #tblclass i.fa:hover { color: #0089b3; }
    #tblclass i.Icondelclass { color: #dc2626 !important; }

    #tblclass_wrapper .dataTables_length label,
    #tblclass_wrapper .dataTables_filter label,
    #tblclass_wrapper .dataTables_info,
    #tblclass_wrapper .dataTables_paginate {
        font-size: 13px;
        color: #475569;
    }
    #tblclass_wrapper .dataTables_filter input {
        border: 1.5px solid #e2e8f0;
        border-radius: 6px;
        padding: 6px 10px;
        height: 34px;
        outline: none;
    }
    #tblclass_wrapper .dataTables_filter input:focus {
        border-color: #0089b3;
        box-shadow: 0 0 0 3px rgba(0,137,179,.1);
    }
    #tblclass_wrapper .dataTables_length select {
        border: 1.5px solid #e2e8f0;
        border-radius: 6px;
        padding: 5px;
        height: 34px;
    }
    #tblclass_wrapper .dataTables_paginate .paginate_button {
        border: 1px solid #e2e8f0 !important;
        border-radius: 6px !important;
        padding: 6px 12px !important;
        margin: 0 2px;
        color: #475569 !important;
        background: #fff !important;
    }
    #tblclass_wrapper .dataTables_paginate .paginate_button.current {
        background: linear-gradient(135deg, #0089b3, #006d8f) !important;
        color: #fff !important;
        border-color: #0089b3 !important;
    }
    #tblclass_wrapper .dataTables_paginate .paginate_button:hover {
        background: #f1f5f9 !important;
        color: #0089b3 !important;
    }

    #classeModal .modal-content {
        border: none;
        border-radius: 12px;
        box-shadow: 0 25px 60px rgba(0,0,0,.15);
    }
    #classeModal .modal-header {
        background: linear-gradient(135deg, #0089b3, #006d8f);
        border-radius: 12px 12px 0 0;
        padding: 16px 24px;
        border: none;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }
    #classeModal .modal-title {
        color: #fff;
        font-size: 15px;
        font-weight: 600;
        letter-spacing: .2px;
    }
    #classeModal .modal-header .close {
        color: rgba(255,255,255,.6);
        opacity: 1;
        text-shadow: none;
        font-size: 22px;
    }
    #classeModal .modal-header .close:hover { color: #fff; opacity: 1; }
    #classeModal .modal-body {
        padding: 24px;
        font-family: 'Roboto', sans-serif;
    }
    #classeModal .form-group { margin-bottom: 14px; }
    #classeModal .form-group label {
        font-size: 12px;
        font-weight: 600;
        color: #475569;
        margin-bottom: 5px;
        text-transform: uppercase;
        letter-spacing: .4px;
    }
    #classeModal .form-control {
        border: 1.5px solid #e2e8f0;
        border-radius: 6px;
        box-shadow: none;
        font-size: 13px;
        padding: 8px 12px;
        height: 38px;
        transition: border-color .2s, box-shadow .2s;
        color: #0f172a;
    }
    #classeModal .form-control:focus {
        border-color: #0089b3;
        box-shadow: 0 0 0 3px rgba(0,137,179,.1);
    }
    #classeModal .modal-footer {
        padding: 14px 24px;
        border-top: 1px solid #f1f5f9;
        display: flex;
        justify-content: flex-end;
        gap: 10px;
    }
    #classeModal .modal-footer .btn {
        border-radius: 6px;
        font-size: 13px;
        font-weight: 500;
        padding: 9px 20px;
        transition: all .2s;
    }
    #classeModal .modal-footer .btn-primary {
        background: linear-gradient(135deg, #0089b3, #006d8f);
        border: none;
        color: #fff;
    }
    #classeModal .modal-footer .btn-primary:hover {
        background: linear-gradient(135deg, #006d8f, #004d66);
    }
    #classeModal .modal-footer .btn-default {
        background: #fff;
        border: 1.5px solid #e2e8f0;
        color: #475569;
    }
    #classeModal .modal-footer .btn-default:hover {
        background: #f1f5f9;
        border-color: #cbd5e1;
    }
</style>

<div class="row" id="div_Scroll">
    <div class="col-lg-12">
        <div class="card-modern">
            <div class="card-header-modern">
                <span class="header-icon"><i class="bi bi-clock-history"></i></span>
                <span class="header-title">View Shift</span>
                <button class="btn btn-register openModal"><i class="bi bi-plus-lg"></i> Add Shift</button>
            </div>
            <div class="card-body-modern">
                @Html.Partial("~/Views/Shared/SuccessError.cshtml")
                <table class="table table-striped table-bordered table-hover table-condensed dt-responsive" id="tblclass">
                    <thead>
                        <tr>
                            <th>Class</th>
                            <th>Shift In</th>
                            <th>Shift Out</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<input type="hidden" id="hdnId" value="0" />
<div class="modal fade" id="classeModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="exampleModalLabel">Add/Update Shift</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
            <div class="modal-body">
                @Html.Partial("~/Views/Shared/Modal_SuccessError.cshtml")
                <div class="row">
                    <div class="col-md-4">
                        <div class="form-group">
                            <label>Class:<span style="color:red;">*</span></label>
                            @Html.DropDownList("ddlClassList", (IList<SelectListItem>)ViewBag.ClassList, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-group">
                            <label for="recipient-name" class="control-label">Shift In:<span style="color:red;">*</span></label>
                            <select id="ddlShiftInList" class="form-control">
                                <option value="0">Select</option>
                                <option value="7:00">7:00</option>
                                <option value="7:30">7:30</option>
                                <option value="8:00">8:00</option>
                                <option value="8:30">8:30</option>
                                <option value="9:00">9:00</option>
                                <option value="9:30">9:30</option>
                                <option value="10:00">10:00</option>
                                <option value="10:30">10:30</option>
                                <option value="11:00">11:00</option>
                                <option value="11:30">11:30</option>
                                <option value="12:00">12:00</option>
                                <option value="12:30">12:30</option>
                                <option value="13:00">13:00</option>
                                <option value="13:30">13:30</option>
                                <option value="14:00">14:00</option>
                                <option value="14:30">14:30</option>
                            </select>
                           
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-group">
                            <label for="recipient-name" class="control-label">Shift Out:<span style="color:red;">*</span></label>
                        
                            <select id="ddlShiftOutList" class="form-control">
                                <option value="0">Select</option>
                                <option value="12:00">12:00</option>
                                <option value="12:30">12:30</option>
                                <option value="13:00">13:00</option>
                                <option value="13:30">13:30</option>
                                <option value="14:00">14:00</option>
                                <option value="14:30">14:30</option>
                                <option value="15:00">15:00</option>
                                <option value="15:30">15:30</option>
                                <option value="16:00">16:00</option>
                                <option value="16:30">16:30</option>
                                <option value="17:00">17:00</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-primary" id="btnSave">Save</button>
                    </div>
                </div>
            </div>
</div>

@section body_scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $("#ddlClassList").select2({
                placeholder: "Select"
            });
            $("#ddlShiftInList").select2({
                placeholder: "Select"
            });
            $("#ddlShiftOutList").select2({
                placeholder: "Select"
            });
            fn_Bindclasstbl("0", "Select");
        });

        $(document).on("click", ".openModal", function () {
            HideSuccErrDiv();
            ModalHideSuccErrDiv();
            fn_resetform();
            $("#classeModal").modal();
        });

        $(document).on("click", ".Iconeditclass", function () {
            var _ClassId = $(this).attr('data-ShiftID');
            $("#hdnId").val(_ClassId);
            fn_Bindclasstbl(_ClassId, "SelectByID");
        });

        $(document).on("click", ".Icondelclass", function () {
            if (confirm("Are you sure want to delete?")) {
                var _DesID = $(this).attr('data-ShiftID');
                fn_delClass(_DesID);
            }
        });

        $(document).on("click", "#btnSave", function () {
            ModalHideSuccErrDiv();
            var _ShiftID = $.trim($("#hdnId").val());
            var _ClassID = $('#ddlClassList').val();
            var _ShiftIn = $("#ddlShiftInList option:selected").text();
            var _Shiftout =  $("#ddlShiftOutList option:selected").text();
          
            if (_ClassID == '' || _ClassID == '-1' || _ClassID == null) {
                ModalPopupError("Please select Class.", "Error", "M_errorBlock");
                return false;
            }
            if (_ShiftIn == '' || _ShiftIn == "0") {
                ModalPopupError("Please enter Shift In.", "Error", "M_errorBlock");
                return false;
            }
            if (_Shiftout == '' || _Shiftout == "0") {
                ModalPopupError("Please enter Shift Out.", "Error", "M_errorBlock");
                return false;
            }

            $.ajax({
                type: "POST",
                url: '@Url.Action("saveShiftMaster", "MasterERP")',
                data: JSON.stringify({ ShiftID: _ShiftID,ClassID:_ClassID ,ShiftFrom: _ShiftIn, ShiftTo: _Shiftout }),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                cache: false,
                success: function (response) {
                    if (response == "InsertSuccessful") {
                        fn_resetform();
                        ModalPopupError("Class Inserted successfully.", "Success", "M_errorBlock");
                        fn_Bindclasstbl("0", "Select");
                    }
                    else if (response == "UpdateSuccessful") {
                        fn_resetform();
                        ModalPopupError("Class updated successfully.", "Success", "M_errorBlock");
                        fn_Bindclasstbl("0", "Select");
                    }
                    else {
                        alert("Error. Try again latet.");
                        return false;
                    }
                },
                error: function (error) {
                    console.error(error);
                    //FormSuccessError(error, "Error", "errorBlock");
                    return false;
                }
            });
        });

        function fn_Bindclasstbl(_ClassId, Type) {
            $.ajax({
                type: "POST",
                url: '@Url.Action("viewShiftMaster", "MasterERP")',
                data: JSON.stringify({ ShiftID: _ClassId, Type: Type }),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                cache: false,
                success: function (data) {
                    if (Type == "Select") {
                        $('#tblclass tbody').empty();
                        $.each(data, function (rowIndex, val) {
                            var row = '';
                            row += '<tr>';
                            row += '<td>' + val.ClassName + '</td>';
                            row += '<td>' + val.ShiftFrom + '</td>';
                            row += '<td>' + val.ShiftTo + '</td>';
                            row += '<td><i class="fa fa-edit Iconeditclass" data-ShiftID="' + val.ShiftID + '"></i><i class="fa fa-trash-o Icondelclass" data-ShiftID="' + val.ShiftID + '"></i></td>';
                            row += '</tr>';
                            $('#tblclass tbody').append(row);
                        });
                        $('#tblclass').dataTable();
                    }

                    if (Type == "SelectByID") {
                        $('#btnSave').text("Update");
                        $('#ddlClassList').select2("val", data[0].ClassID);
                        $("#ddlShiftInList").select2("val", data[0].ShiftFrom);
                        $("#ddlShiftOutList").select2("val", data[0].ShiftTo);
                        $("#classeModal").modal();
                    }
                },
                error: function (error) {
                    console.error(error);
                    //FormSuccessError(error, "Error", "errorBlock");
                    return false;
                }
            });
        }

        function fn_delClass(ShiftID) {
            $.ajax({
                type: "POST",
                url: '@Url.Action("deleteShiftMaster", "MasterERP")',
                data: JSON.stringify({ ShiftID: ShiftID}),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                cache: false,
                success: function (response) {
                    if (response == "deleteSuccessful") {
                        fn_Bindclasstbl("0", "Select");
                        FormSuccessError("Class deleted successfully.","Success", "errorBlock");
                        //alert("Class deleted successfully.");
                    }
                    else {
                        alert("Error. Try again latet.");
                        return false;
                    }
                },
                error: function (error) {
                    console.error(error);
                    //FormSuccessError(error, "Error", "errorBlock");
                    return false;
                }
            });
        };

        function fn_resetform() {
            $("#hdnId").val('0');
            $('#ddlClassList').select2("val", '');
            $("#ddlShiftInList").select2("val", '');
            $("#ddlShiftOutList").select2("val", '');
            $('#btnSave').text("Save");
        }
    </script>
}


